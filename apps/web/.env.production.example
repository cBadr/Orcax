# ==============================================================================
# PRODUCTION .env for the Web app — copy to apps/web/.env on the server
# ==============================================================================

# Public API base URL (no trailing slash). Must be reachable from the browser.
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Optional: default title shown before the API responds with the real brand.
NEXT_PUBLIC_SITE_NAME=Platform
